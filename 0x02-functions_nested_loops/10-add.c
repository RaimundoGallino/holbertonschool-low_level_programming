#include "holberton.h"
/**
 * add - add two integers and returns the result
 * @n: The number to print
 * @n1: second number to print
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */

int add(int n, int n1)
{
	int i = n + n1;

	return (i);
}
