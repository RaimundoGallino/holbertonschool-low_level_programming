#include "holberton.h"

/**
 * mul - multiplies a * b
 * @a: number to multiply
 * @b: multiplier
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int mul(int a, int b)
{
	int z = a * b;

	return (z);
}
