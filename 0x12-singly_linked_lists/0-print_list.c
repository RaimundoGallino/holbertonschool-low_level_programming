#include <stdio.h>
#include <stdlib.h>
#include "lists.h"

size_t print_list(const list_t *h)
{
	const list_t *contador = h;

	next = NULL;

	if (h)
	{
		while (contador->next)
		{
			contador = contador->next;
			
			if (str == NULL)
			{
				printf ("%s", [0] (nil));
			}
			printf("%s", str);
		}
	}
}